{
    "name": "your-log",
    "version": "1.0.0",
    "description": "",
    "private": true,
    "main": "index.js",
    "scripts": {
        "dev": "concurrently -n API,WEB -c blue,green \"pnpm --filter api dev\" \"wait-on http://localhost:3001 && pnpm --filter web dev\"",
        "build": "turbo run build",
        "format": "prettier --write ."
    },
    "packageManager": "pnpm@10.29.2",
    "devDependencies": {
        "concurrently": "^9.2.1",
        "eslint-config-prettier": "^10.1.8",
        "prettier": "^3.8.1",
        "turbo": "^2.8.3",
        "wait-on": "^9.0.4"
    }
}
